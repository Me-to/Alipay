server:
  port: 8081
spring:
  application:
    name: zhifu_pay
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/zhifu_pay?useUnicode=true&characterEncoding=utf8&serverTimezone=UTC
    username: root
    password: zhangqian
    type: com.alibaba.druid.pool.DruidDataSource
mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
alipay:
  app_id: #######
  gatewayUrl: https#://openapi.alipaydev.com/gateway.do
  merchant_private_key: ####
  alipay_public_key: ####
  notify_url: http#://qian1.vip.qydev.com/notify
  return_url: http#://qian1.vip.qydev.com/pay/ok
  sign_type: RSA2
  charset: utf-8
  rabbitmq:
    host: localhost # rabbitmq的连接地址
    port: 5672 # rabbitmq的连接端口号
    virtual-host: /qian # rabbitmq的虚拟host
    username: qian # rabbitmq的用户名
    password: qian # rabbitmq的密码
    publisher-confirms: true #如果对异步消息需要回调必须设置为true

